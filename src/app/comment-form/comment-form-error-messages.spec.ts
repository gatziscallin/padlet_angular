import { CommentFormErrorMessages } from './comment-form-error-messages';

describe('CommentFormErrorMessages', () => {
  it('should create an instance', () => {
    expect(new CommentFormErrorMessages()).toBeTruthy();
  });
});
