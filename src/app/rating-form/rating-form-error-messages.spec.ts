import { RatingFormErrorMessages } from './rating-form-error-messages';

describe('RatingFormErrorMessages', () => {
  it('should create an instance', () => {
    expect(new RatingFormErrorMessages()).toBeTruthy();
  });
});
